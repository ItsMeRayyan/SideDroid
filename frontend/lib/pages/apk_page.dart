import 'package:flutter/material.dart';

class ApkPage extends StatelessWidget {
  const ApkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("APK Installer"),
    );
  }
}